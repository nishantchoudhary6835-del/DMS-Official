# Dependencies
node_modules/

# Expo build + cache artefacts
.expo/
dist/
web-build/
webdist/
*.orig.*

# Native build outputs (this project uses Expo managed workflow)
/ios
/android

# Real environment values — only .env.example is committed
.env
.env.local
.env.*.local

# Logs
npm-debug.*
yarn-debug.*
yarn-error.*
.devweb.log
.devweb.err.log

# Backend API specs and bug writeups — kept locally as the record of what
# this client was built against, but deliberately not tracked: they are the
# backend team's documents, they churn constantly, and they were crowding
# the repo. Still recoverable from history (they were tracked up to b55cea8).
# Use `git add -f` if one ever genuinely needs committing.
docs/

# OS noise
.DS_Store
Thumbs.db
