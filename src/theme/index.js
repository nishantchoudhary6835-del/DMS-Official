import { colors } from './colors';
import { fonts, fontAssets } from './fonts';
import { patterns } from './patterns';
import { spacing, radius, elevation, spine } from './spacing';
import { typography } from './typography';

export const theme = {
  colors,
  spacing,
  radius,
  elevation,
  spine,
  typography,
  fonts,
  patterns,
};

export {
  colors,
  spacing,
  radius,
  elevation,
  spine,
  typography,
  fonts,
  fontAssets,
  patterns,
};
